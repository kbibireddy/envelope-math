{
  "name": "envelope-math",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "check": "npm run test:coverage && npm run build"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.11",
    "vite": "^8.2.2",
    "vitest": "^4.1.11"
  }
}
